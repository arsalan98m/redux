/ *** Thunk Middleware *** /

How to make AJAX request?
There are bunch of libraries to do that like of most thing it all depends on how extremely needs are if we have very simple need redux-thunk is probably the way to go because it is incredibly simple if we are doing really sophisticated things we might want to like redux-saga or anyother tools.
redux-thunk is preety easy

basically redux-thunk allows us to do dispatch functions that will eventually dispatch actions so we tell the redux-thunk  lets dispatch this function that will get the data from the api and it will go and dispatch the action once get the data return back; 
